{% extends "::application.html.twig" %} 

{% block title %}Mes cotisations{% endblock %}

  {% block body %}
    <div id="page-content">
      <div id='wrap'>
        <div id="page-heading">
          <ol class="breadcrumb">
            <li><a href="{{ path('application_main_homepage') }}">Plateforme</a></li>
            <li class="active">Cotisations</li>
          </ol>
        </div>

        <div class="container">
          <div class="jumbotron">
            <h3>Pourquoi Cotiser ?</h3>
            Cotiser permet à l'association d'évoluer et de subvenir aux différents besoins financiers comme la maintenance et l'hébergement de cette plateforme ou encore l'organisation d'évènements !
            Cela vous donne également des avantages comme la possibilité de proposer des offres aux ZZ et de consulter le profil des personnes !
          </div>
          <h1>Mes cotisations</h1>
          <div class="row">
            <div class="pannel-group">
                {% if (cotisDispo | length > 0) or (cotisAttente | length > 0) %}
                    {% set i = 0 %}
                    {% for dateDispo in cotisDispo %}
                      <div class="col-xs-6">
                        <div class="panel panel-orange">
                          <div class="panel-heading">
                            <h4>Cotisation {{ dateDispo }}</h4>
                          </div>
                          <div class="panel-body">
                            <p class="text-info">La cotisation pour l'année {{ dateDispo }} est ouverte ! Vous pouvez cotiser dès maintenant pour profiter aux plus vite des avantages !</p>
                            <div class="col-xs-12 ">
                                {{ form_start(forms[i]) }}
                              <div class="form-group">
                              {{ form_label(forms[i].typeCotisation, "Type de cotisation*", { 'label_attr' : { 'class' : 'col-sm-offset-1 col-sm-4 control-label' }})}}
                                {{ form_widget(forms[i].typeCotisation, { 'attr' : { 'text-align' : 'center' } }) }}
                              </div>
                                <div class="form-group col-xs-offset-4 ">
                                  {{ form_errors(forms[i]) }}
                                  {{ form_rest(forms[i]) }}
                                </div>
                                {{ form_end(forms[i]) }}
                          </div>
                        </div>
                      </div>
                      </div>
                      {% set i = i + 1 %}
                    {% endfor %}
                    {% for dateAttente in cotisAttente %}
                        <div class="col-xs-6">
                          <div class="panel panel-orange">
                            <div class="panel-heading">
                              <h4>Cotisation {{ dateAttente.getYear | date("Y")  }}</h4>
                            </div>
                            <div class="panel-body">
                              <p class="text-info">Votre cotisation est en attente de validation, si vous ne souhaitez pas payer en ligne, vous pouvez régler :</p>
                                <ul>
                                <li>Par chèque à l'ordre de l'<b>Association des Anciens Elèves de l'ISIMA</b></li>
                                <li>Par virement à l'IBAN suivant : <b>XXXXXXXXXXXXXXXXXXXXXXXXXXXXX</b></li>
                              </ul>
                              <div class="col-xs-offset-4">
                              <form action="https://www.paypal.com/cgi-bin/webscr" method="post" target="_top">
                              <input type="hidden" name="cmd" value="_s-xclick">
                              <input type="hidden" name="hosted_button_id" value="M2LFT36X5JHZC">
                              <input type="hidden" name="on0" value="Cotisation">
                              <input type="hidden" name="os0" value="Membre 10€">
                              <input type="hidden" name="currency_code" value="EUR">
                              <button class="btn-primary btn">Paiement Paypal</button>
                              </form>
                              </div>
                            </div>
                          </div>
                    {% endfor %}
                    {% for dateOK in cotisOK %}
                      <div class="col-xs-6">
                        <div class="panel panel-midnightblue">
                          <div class="panel-heading">
                            <h4>Cotisation {{ dateOK }}</h4>
                          </div>
                          <div class="panel-body">
                            <p class="text-info">Vous avez déjà cotisé pour l'année {{ dateOK }}, merci de votre aide !</p>
                          </div>
                        </div>
                      </div>
                    {% endfor %}
                {% else %}
                  <p class="text-danger">Aucune cotisation n'est active et il n'est pas possible de cotiser actuellement.</p>
                {% endif %}
            </div>
          </div>
        </div>

        <div class="container">
          <h1>Historique</h1>
          <div class="row">
            <div class="col-xs-12">
              <div class="panel panel-midnightblue">
                <div class="panel-heading">
                  <h4>Historique des cotisations</h4>
                  <div class="options">

                  </div>
                </div>
                <div class="panel-body">
                    {% if app.user.cotisations|length > 0 %}
                      <table class="table">
                        <thead>
                        <tr>
                          <th>#</th>
                          <th>Année</th>
                          <th>Type</th>
                          <th>Prix</th>
                          <th>Statut</th>
                          <th>Facture</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% set cotisationNumbers = 1 %}
                        {% for cotisation in app.user.cotisations|reverse %}

                          <tr>
                            <td>{{ cotisationNumbers }}</td>
                            <td>{{ cotisation.year|date("Y") }}</td>
                            <td>{{ cotisation.typeCotisation.name }}</td>
                            <td>{{ cotisation.priceCotisation }} €</td>
                            <td>{% if cotisation.invoice.payed %}<button class="btn btn-success">Payé</button>{% else %}<button class="btn btn-danger">En attente de paiement</button>{% endif %}</td>
                            <td><a href="{{ path("application_cotisation_invoice_get", { "id" : cotisation.invoice.id }) }}"><i class="fa fa-eye"></i></a>    {% if not cotisation.invoice.payed %}
                                <a href="{{ path("application_cotisation_delete", { "id" : cotisation.id }) }}"><span class="glyphicon glyphicon-trash"></span></i></a>
                                {% endif %}</td>
                          </tr>
                            {% set cotisationNumbers = cotisationNumbers + 1 %}
                        {% endfor %}
                        </tbody>
                      </table>
                    {% else %}
                      <p class="text-danger"><strong>Aucune cotisation n'a été trouvé pour votre compte. N'hésitez pas à effectuer une demande de cotisation.</strong></p>
                    {% endif %}


                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    </div>

  {% endblock %}